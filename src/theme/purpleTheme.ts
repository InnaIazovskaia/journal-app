import { createTheme } from "@mui/material/styles";

export const purpleTheme = createTheme({
  palette: {
    primary: {
      main: "#2e3151",
    },
    secondary: {
      main: "#6c71a9",
    },
    error: {
      main: "#f91f6d",
    },
  },
});
